import 'package:flutter/material.dart';

class ToggleSwitch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('aqui va el switch'),
    );
  }
}
